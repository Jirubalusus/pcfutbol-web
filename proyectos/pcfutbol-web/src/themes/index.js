import classic from './classic';
import modern from './modern';
import glass from './glass';
import midnight from './midnight';

export const themes = {
  classic,
  modern,
  glass,
  midnight,
};

export const themeList = [classic, modern, glass, midnight];
export const DEFAULT_THEME = 'classic';
