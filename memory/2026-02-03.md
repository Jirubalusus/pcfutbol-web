# 2026-02-03 — Sesión noche (PC Fútbol Web)

## Cambios realizados

### Explorar Fichajes — Pestaña "Resto del Mundo"
- Añadida tercera pestaña en Explorar: Europa | Sudamérica | **Resto del Mundo** 🌏
- 4 ligas añadidas a `AVAILABLE_LEAGUES` en scoutingSystem.js: MLS, Saudi Pro League, Liga MX, J1 League
- Nueva constante `WORLD_COUNTRIES` + función `getContinent()` en TransfersV2.jsx

### Plantilla — Tags "En venta" en móvil
- Movidos tags (En venta, EN CESIÓN, Se retira) fuera del `<span className="name">` a un `<div className="player-tags">` separado
- Ahora aparecen en línea debajo del nombre, no inline truncando

### Modal de oferta — Rediseño visual
- `.negotiation-section`: gradiente sutil, bordes redondeados (sin barra lateral)
- `.section-header`: línea separadora debajo
- Labels: uppercase, tracking, más ligeros
- MoneyInput: botones +/- más grandes, mejor contraste
- Barras probabilidad: 10px altura, fondo tintado, dentro de card oscuro
- Botones contrato: bordes redondeados, gradiente azul activo
- Nuevo `.offer-summary-box`: card con líneas y total destacado
- Nuevo `.btn-send-offer`: verde, prominente
- Nuevo `.offer-divider`: gradiente sutil
- Nuevo `.offer-hint`: texto hint centrado

### Facilities — Bug [object Object]
- `facility.icon` es componente React JSX, no string
- Fix: título del mensaje cambiado de `${facility.icon} ${facility.name} mejorado` a `${facility.name} mejorado`

### Comercial — No escala por liga
- Pol quiere que el Comercial sea igual para todos los equipos
- Eliminado `econMult` del cálculo de benefits en Facilities.jsx
- Valores fijos: Básico €950K, Activo €2.7M, Profesional €5.3M, Premium €8.9M por temporada

### Calendario — Nombres completos y alineación
- Eliminado truncado de `stripName` (ya no limita a 16 chars)
- Eliminado `text-overflow: ellipsis` del CSS
- Ambos equipos (home/away) ahora alineados a la izquierda
- Fuente móvil aumentada de 0.72rem a 0.78rem

### Táctica — Error "t is not defined"
- `TacticModal` no tenía acceso a `useTranslation()` pero usaba `t(...)`
- Fix: añadido `const { t } = useTranslation()` al componente
- Renombrado variable `t` → `opt` en `.map()` de tacticOptions para evitar shadowing

## Git
- Commit: `UI fixes: Resto del Mundo tab, plantilla tags, transfer modal redesign, facility msg fix, calendar names, commercial unscaled`
- Push + deploy a GitHub Pages

## Pendiente (de HEARTBEAT.md)
- Firebase Security Rules para `contrarreloj_ranking`
- Restricción API key Firebase
- Firebase index para ranking queries
- SoFIFA: equipos mal clasificados (Elche, Levante, etc. en LaLiga en vez de Segunda)
